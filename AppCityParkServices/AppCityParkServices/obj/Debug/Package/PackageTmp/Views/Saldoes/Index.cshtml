@model IEnumerable<AppCityParkServices.Models.Saldo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Saldo1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Saldo1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SaldoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SaldoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SaldoId })
        </td>
    </tr>
}

</table>
